"""Service layer: business logic for each studio module.

Each service turns user input into LLM messages, calls the client, and returns
a result. Services are the only place business logic lives.
"""
